apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver
spec:
  template:
    spec:
      containers:
      - env:
        - name: SERVERCONTNET
          value: PROD - WebServer V1
        name: simpleserver