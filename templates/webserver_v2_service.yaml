apiVersion: v1
kind: Service
metadata:
  name: webserverv2
spec:
  ports:
    - name: http-web
      port: {{ .Values.webserverv2.servicePort }}
      protocol: TCP
      targetPort: {{ .Values.webserverv2.containerPort }}
  selector:
    app: webserver
    version: v2